import styled from "styled-components";
import { Card } from "src/components/Card";

const Wrapper = styled(Card)`
  grid-area: applicationText;
  margin: 0;
  padding: 1.5em;
  font-size: 0.9em;
  font-family: Raleway, "Courier New", Arial, sans-serif;
`;

export default Wrapper;
